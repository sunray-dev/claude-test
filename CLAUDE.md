# Claude Test — Project Instructions

## Session Start

At the start of every session, follow the `clickup-dev-workflow` skill:

1. Fetch tasks from the Backlog list (ID: `901523813899`) filtered by status `unstarted`
2. Present them ordered by priority (urgent → high → normal → low)
3. Ask the user which ticket they want to work on before doing anything else

Do this automatically — do not wait for the user to ask.
